{
  "extends": ["//"],
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "inputs": ["src/**"],
      "outputs": [".next/**", "out/**"]
    },
    "schema:gen": {
      "inputs": ["../schemas/**/*.json"],
      "outputs": ["src/generated/**"]
    }
  }
}

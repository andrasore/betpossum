{
  "extends": ["//"],
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "inputs": ["src/**"],
      "outputs": [".next/**", "out/**"]
    },
    "proto:gen": {
      "inputs": ["../proto/**/*.proto"],
      "outputs": ["src/generated/**"]
    }
  }
}

{
  "extends": ["//"],
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "inputs": ["src/**"],
      "outputs": [".next/**"]
    },
    "proto:gen": {
      "inputs": ["../proto/**/*.proto"],
      "outputs": ["src/generated/**"]
    }
  }
}

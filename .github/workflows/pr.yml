name: PR checks

on:
  pull_request:
  push:
    branches: [main]

env:
  COMPOSE_BAKE: 'true'

jobs:
  # Everything that runs against the source tree without a built Docker image:
  # typecheck, lint, the JS/Python build (regenerates bindings) and unit tests.
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.8

      - uses: actions/setup-node@v4
        with:
          node-version: '25'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - uses: astral-sh/setup-uv@v7
        with:
          version: '0.11.30'

      - name: Cache node_modules
        id: cache-node
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            frontend/node_modules
            services/core/node_modules
          key: node-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Cache Python venvs
        id: cache-venv
        uses: actions/cache@v4
        with:
          path: |
            services/odds/.venv
            services/notifications/.venv
            services/stats/.venv
          key: venv-${{ runner.os }}-py3.14-v3-${{ hashFiles('services/*/pyproject.toml', 'services/*/uv.lock') }}

      - name: Cache turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: turbo-${{ runner.os }}-

      # --force so `turbo run init` actually executes: init has no cached
      # outputs, so a turbo cache hit replays its logs without recreating the
      # .venv directories. On this cache-miss path the venvs must really exist
      # before actions/cache saves them, or the next run restores empty dirs.
      - name: Install deps (cache miss only)
        if: steps.cache-node.outputs.cache-hit != 'true' || steps.cache-venv.outputs.cache-hit != 'true'
        run: pnpm install && pnpm exec turbo run init --force

      - run: pnpm run typecheck
      - run: pnpm run lint
      - run: pnpm run build
      - run: pnpm run test

  # Build every service image once. On main the images are pushed to GHCR under
  # an immutable :<sha> staging tag, so the push job can promote them by digest
  # instead of rebuilding. On PRs we only warm the per-service BuildKit GHA
  # cache (docker-compose.ci.yml) that the e2e job reuses.
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build service images (warm cache)
        if: github.ref != 'refs/heads/main'
        run: >
          docker compose -f docker-compose.yml -f docker-compose.ci.yml
          build core odds notifications stats nginx keycloak bots

      - name: Log in to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve image repo
        if: github.ref == 'refs/heads/main'
        run: echo "IMAGE_REPO=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      # Two-step build then push: `build --push` is not honored by the Compose
      # version on the runner under bake delegation (it loads the images into
      # the local engine instead of pushing). `compose build` followed by
      # `compose push` reliably publishes the :<sha> staging tags.
      - name: Build and push staging images
        if: github.ref == 'refs/heads/main'
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker compose -f docker-compose.yml -f docker-compose.ci.yml -f docker-compose.push.yml build core odds notifications stats nginx keycloak bots
          docker compose -f docker-compose.yml -f docker-compose.ci.yml -f docker-compose.push.yml push core odds notifications stats nginx keycloak bots

  # Boot the full stack from the just-built images and drive it with Playwright.
  # global-setup runs `compose up -d --build`; with CI set it layers in
  # docker-compose.ci.yml, so the build resolves from the cache the build job
  # populated.
  e2e:
    needs: [test, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.8

      - uses: actions/setup-node@v4
        with:
          node-version: '25'

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            frontend/node_modules
            services/core/node_modules
          key: node-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install deps
        run: pnpm install

      - name: Install Chromium
        uses: browser-actions/setup-chrome@v2

      - run: pnpm run e2e

      - name: Upload e2e artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-results
          path: e2e/test-results/
          if-no-files-found: ignore
          retention-days: 14

  # Promote the e2e-validated :<sha> staging images to :latest. This is a
  # registry-side manifest copy (docker buildx imagetools create) — no rebuild,
  # no layer transfer, so it runs in seconds. Only on pushes to main.
  # TODO(temporary): push-only gating is a stopgap — also run this on merged
  # PRs once the workflow is finalized.
  push:
    needs: e2e
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Promote staging images to latest + semver release tag
        env:
          IMAGE_TAG: ${{ github.sha }}
          RUN_NUMBER: ${{ github.run_number }}
        run: |
          repo="ghcr.io/${GITHUB_REPOSITORY,,}"
          # Alongside the human-facing moving :latest tag, stamp an immutable
          # semver release tag. Flux's ImagePolicy (semver range) ranks releases
          # by semver precedence, so the tag has to parse as semver for it to
          # order builds at all. run_number is monotonic and never resets, so a
          # higher patch is always a newer build (it also counts PR runs, leaving
          # gaps in the series — ordering is what matters, not density).
          #
          # Bump the 0.1 series here by hand; nothing derives it from a
          # package.json, as no single root version exists to derive it from.
          #
          # The git sha is deliberately absent: a tag cannot carry semver build
          # metadata (docker forbids '+'), and a '-<sha7>' suffix would instead
          # make it a PRERELEASE, which sorts *before* the plain version and
          # inverts the ordering. The :<sha> staging tag on this same digest
          # carries that traceability instead.
          #
          # Registry-side copy — no rebuild.
          release="0.1.${RUN_NUMBER}"
          for name in core odds notifications stats frontend keycloak bots; do
            docker buildx imagetools create \
              -t "$repo/$name:latest" \
              -t "$repo/$name:$release" \
              "$repo/$name:$IMAGE_TAG"
          done

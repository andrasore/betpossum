{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    },
    "proto:gen": {
      "inputs": ["../../proto/**/*.proto"],
      "outputs": ["src/generated/**"]
    },
    "typecheck": {
      "dependsOn": ["build"],
      "inputs": [
        "**/*.ts",
        "**/*.tsx",
        "**/*.py",
        "tsconfig.json",
        "pyproject.toml"
      ],
      "outputs": []
    },
    "init": {
      "cache": false
    }
  }
}
